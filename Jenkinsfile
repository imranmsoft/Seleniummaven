pipeline {
    agent any

    stages {
        stage('Hello') {
           
            steps {
               /usr/share/maven/bin/mvn package 
                
                         }
        }
    }
}
